<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
  <%= render 'layouts/navbar' %>
  <div class="sidenav">
    <ul class="list-group">
      <li class="list-group-item">
        <div class="dropdown">
          <button class="btn dropdown-toggle text-info font-weight-bold" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%=@workspace.name %>
          </button>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
           <p><%= link_to t('workspaces.edit.Edit'), edit_workspace_path(@workspace), method: 'get', class:'text-info'%></p>
           <p> <a class="dropdown-item" href="#"><%= link_to t('workspaces.edit.Remove'), workspace_path(@workspace), method: 'delete', data: { confirm: 'Are you sure?' }, class:'text-info' %></a></p>
           <p style="margin-left: 5px"> <a class="dropdown-item" href="#"><%= link_to t('workspaces.edit.Switch To New Workspce'), workspaces_path, class:'text-info' %></a></p>
         </div>
       </div></li>
       <li class="list-group-item d-flex justify-content-between align-items-center text-info font-weight-bold">
         <%= t('workspaces.edit.Add New Channel') %>
         <span class="badge  badge-pill">
           <%= link_to " #", new_channel_path(:id =>@workspace.id) %></span>
         </li>
         <% if @channels %>
         <% @channels.each do |channel| %>
         <li class="list-group-item">#<%= link_to channel.name, channel_path(channel), class:'font-weight-bold text-info'  %></li>
         <% end %>

         <% else %>
         <% end %>     
         <li class="list-group-item"> <%= link_to t('workspaces.edit.Invite member'), workspace_users_workspace_index_path(@workspace), class:'font-weight-bold text-info' %></li>
       </li> 
     </ul>
   </div>
   <div class="container">
    <div class="row">
      <div class="col-sm-6 offset-3">
       <div class="card">
        <h5 class="card-header info-color white-text text-center text-info">
          <strong> <%= t('workspaces.edit.Edit Workspace') %></strong>
        </h5>
        <div class="card-body text-danger">
         <%= form_for  @workspace do |f| %>
         <%= form_group_for f, :name, label: false do %>
         <%= f.label :name, class: 'text-info text-uppercase' %>
         <div class="Input-group">    
          <%= f.text_field :name, class: 'form-control', placeholder: t('workspaces.edit.Edit Workspace Name') %>
        </div>
        <% end %> 
        <center><div class="form-group"> <%= f.submit class: 'btn btn-info' %>
          <%= link_to t('workspaces.edit.Cancel'), workspace_path, class: "btn btn-info" %>
          <% end %>
        </div></center>
      </div>
    </div>
  </div>
</body>
</html> 




