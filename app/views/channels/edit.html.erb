<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
  <%= render 'navbar' %>
  <div class="sidenav">
    <%= render 'layouts/sidebar' %>
  </div>
  <div class="container" style="margin-top: 80px;">
    <div class="row">
      <div class="col-sm-6 offset-3">
       <div class="card">
        <h5 class="card-header info-color white-text text-center text-info">
          <strong><%= t('channels.edit.Edit Channel') %></strong>
        </h5>
        <div class="card-body">
         <%= form_with model: @channel do |form| %>
         <div class="form-group text-info text-uppercase">
          <%= form.label :name %>
          <%= form.text_field :name, placeholder: t('channels.edit.Edit Channel Name'),class:'form-control' %>
        </div>
        <div class="form-group">
         <%= form.submit class: "btn btn-info" %>
         <%= link_to 'Cancel', channel_path(@channel), class: "btn btn-info" %>  
       </div>
       <% end %>
     </div>
     <script>
      $(function () {
        $('input').iCheck({
          checkboxClass: 'icheckbox_square-blue',
          radioClass: 'iradio_square-blue',
          increaseArea: '20%' 
        });
      });
    </script>
  </div>
</body>
</html> 




