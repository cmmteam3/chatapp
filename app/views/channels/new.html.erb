
<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
body {
  font-family: "Lato", sans-serif;
}

.sidenav {
  height: 100%;
  width: 250px;
  position: fixed;
  z-index: 1;
  top: 58px;
  left: 3px;
  background-color: Lavender;
  overflow-x: hidden;
}





@media screen and (max-height: 450px) {
  .sidenav {padding-top: 15px;}
  .sidenav a {font-size: 18px;}
}
</style>
</head>
<body>
<%= render 'workspaces/navbar' %>
<div class="sidenav">
  <ul class="list-group">
  <li class="list-group-item">
  <div class="dropdown">
  <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color:red">
    <%=@workspace.name %>
  </button>
  <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
   <p style="margin-left: 29px"><%= link_to 'Edit', edit_workspace_path(@workspace), method: 'get'%></p>
    <p style="margin-left: 29px"> <a class="dropdown-item" href="#"><%= link_to 'Remove', workspace_path(@workspace), method: 'delete', data: { confirm: 'Are you sure?' } %></a></p>
   
  </div>
</div></li>
  <li class="list-group-item d-flex justify-content-between align-items-center">
Add New Channel
    <span class="badge  badge-pill">
      <%= link_to " #", new_channel_path(:id =>@workspace.id) %></span>
  </li>
 <% if @channels %>


  <% @channels.each do |channel| %>
    <li class="list-group-item">#<%= link_to channel.name, channel_path(channel)  %></li>
  <% end %>

<% else %>

<% end %>
  
  
</ul>

</div>
<div class="container" style="margin-top: 80px;">
  <div class="row">
  <div class="col-sm-6 offset-3">
 <div class="card">

  <h5 class="card-header info-color white-text text-center ">
    <strong> Add New Channel</strong>
  </h5>
  <div class="card-body">
    <%= form_for(@channel) do |f| %> 
        <div class="form-group has-feedback">
        <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' ,name:"name" %>
      </div>
      <div>
         <%= f.hidden_field :workspace_id, value: @workspace.id ,name:"workspace_id" %>
      </div>
      <div class="form-group">
        <%= f.check_box :privacy, name:'privacy' %>
        <%= f.label :Public %>
             
  </div>
  <div class="col-xs-3">
           <%= f.submit "Save", class: "btn btn-primary btn-block btn-flat" %>
        </div>

      </div>
      
      
      
    <% end %>   
  </div>


<script>
  $(function () {
    $('input').iCheck({
      checkboxClass: 'icheckbox_square-blue',
      radioClass: 'iradio_square-blue',
      increaseArea: '20%' 
    });
  });
</script>
</div>
  
</body>
</html> 


 