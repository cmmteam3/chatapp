<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title></title>
	<link rel="stylesheet" href="">
</head>
<body>
	<%= render 'layouts/navbar' %>
	<div class="container">
		<div class="row">
			<div class="col-sm-6 offset-sm-3">
				<div class="car">
					<div class="card card-header">
						<h4 class="text-center text-info"><%= t'session.new.Login' %></h2>
						</div>
						<div class="card card-body">
							<%= form_for(:session,  url: login_path) do |f| %>
							<div class="form">
								<i class="fa fa-envelope text-info text-uppercase">  <%= t('email') %></i>
								<%= f.text_field :email, class: 'form-control' %>
							</div>
							<div class="form-group text-info text-uppercase">
								<i class="fa fa-lock text-info text-uppercase">  <%= t('password') %></i>
								<%= f.password_field :password, class: 'form-control' %>
							</div>
							<center><div class="form-group">
								<%= f.submit t("session.new.Login"), class: "btn btn-info" %>
								<%= link_to t('session.new.Cancel'), root_path, class: "btn btn-info" %>
								<% end %>
							</div></center>
						</div>
					</div>
				</div>
			</div>
		</div>
	</body>
	</html>